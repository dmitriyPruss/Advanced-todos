import { Box } from '@mui/material';
import { styled } from '@mui/system';
import { FLEXBOX } from '../../../theme';

export const StyledTablePaginationContainer = styled(Box)(() => ({
  flexShrink: FLEXBOX.flexShrinkZero
}));
